//custom history object for React Router
//in order to access it from anywhere inside our project
//needs to be used with a plain Router and not a Browser, Hash... Router
import { createBrowserHistory } from 'history'; 

export default createBrowserHistory();